server:
  port: 9091

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8778/eureka/

zuul:
  prefix: /api
#  Uncomment to disable services automatically registering in Zuul
#  ignoredServices: '*'

  routes:
    echo-service: /hello/**
  #    path: /echo-service-by-dns/**
   #   url: http://echo-service:9098

management:
  security:
    enabled: false
    
 ---
 
 spring:
  profiles: docker
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://docker:8778/eureka/

zuul:
  prefix: /api
#  Uncomment to disable services automatically registering in Zuul
#  ignoredServices: '*'

  routes:
    echo-service: /hello/**


#management:
#  security:
 #   enabled: false
 server:
  port: 19091